
import { Door } from './door';

/* A monster spawner */
export class Spawn {
    constructor(x, y) {
        this.sprite = null;
        this.x = x;
        this.y = y;
    }

    update(dt) {
    }
};

